# All API requests are handled by the MVC router via root .htaccess → public/index.php
# Old direct-access files here are disabled (.disabled) or superseded by controllers.
# No deny rules — root .htaccess rewrites everything to public/ anyway.